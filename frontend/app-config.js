/* ============================================================
   CONTATECK · app-config.js
   OT-0005 · Config del entorno del frontend (no de credenciales).
   Por default usa el backend de PRODUCCIÓN en Railway — así el
   sitio real (GitHub Pages) sigue funcionando exactamente igual
   que antes, sin que nadie tenga que tocar nada.

   Para pruebas en tu compu contra un backend LOCAL (npm start en
   backend/), descomenta la línea de abajo. Vuélvela a comentar
   (o bórrala) antes de subir cambios — nunca debe llegar a
   producción apuntando a localhost.
   ============================================================ */
window.APP_CONFIG = {
  // BACKEND_URL: "http://localhost:8080",
};
